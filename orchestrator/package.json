{
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./input.css -o ./static/output.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./input.css -o ./static/output.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4"
  }
}
